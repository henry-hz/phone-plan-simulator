Before using this small code, you have to setup the Boost libraries. You can build your own PhonePlans, inserting the desired use statistics, so it will run a montecarlo simulation to compare them. Use R-statistics to plot the result in a beautiful histogram.
